package com.lvn.codementor.ai.repository.domain;

/** Repository visibility. A PRIVATE repo requires a valid provider token (BR-REP-003). */
public enum RepositoryVisibility {
    PUBLIC,
    PRIVATE
}
